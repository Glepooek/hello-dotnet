<UserControl x:Class="Digihail.Controls.LegendControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="root"
             >
    <ItemsControl ItemsSource="{Binding ElementName=root,Path=DataSource}">
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Ellipse x:Name="ellipse" Stroke="#1D1D1D" Fill="{Binding Brush}" Height="12" Width="12" VerticalAlignment="Center">
                        <Ellipse.Effect>
                            <DropShadowEffect BlurRadius="7" Color="#000000" Opacity="0.3" Direction="0" ShadowDepth="1"></DropShadowEffect>
                        </Ellipse.Effect>
                    </Ellipse>
                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontFamily="微软雅黑" Foreground="#CCCCCC" FontSize="14" Margin="10,0,2,0" VerticalAlignment="Center"></TextBlock>
                    <TextBlock Grid.Column="2" FontFamily="微软雅黑" Foreground="#CCCCCC" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Left">
                            <Run Text="："></Run>
                            <Run Text="{Binding Number}"></Run>
                        <TextBlock.Style>
                            <Style TargetType="{x:Type TextBlock}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ElementName=root,Path=IsVisibleNumber}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"></Setter>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ElementName=root,Path=IsVisibleNumber}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <Grid.Style>
                        <Style TargetType="{x:Type Grid}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=root,Path=Orientation}" Value="Horizontal">
                                    <Setter Property="Margin" Value="20,0,0,0"></Setter>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ElementName=root,Path=Orientation}" Value="Vertical">
                                    <Setter Property="Margin" Value="0,0,0,12"></Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="{Binding ElementName=root,Path=Orientation}"></StackPanel>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
    </ItemsControl>
</UserControl>
