<Window x:Class="Test.ListBoxPage.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:local="clr-namespace:Test.ListBoxPage"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Window.Resources>
        <Style x:Key="ItemContainerStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Width" Value="100" />
            <Setter Property="Height" Value="100" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border Margin="5" Background="Gray">
                            <TextBlock HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Text="{Binding Name}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <local:ListBoxPage ItemsSource="{Binding Datas}"
                       LoadEnd="{Binding LoadEnd}"
                       Completed="{Binding Completed}"
                       Loadline="100"
                       ItemContainerStyle="{StaticResource ItemContainerStyle}">
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="BeginLoad">
                <i:InvokeCommandAction Command="{Binding BeginLoadCommand}" />
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <local:ListBoxPage.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel />
            </ItemsPanelTemplate>
        </local:ListBoxPage.ItemsPanel>
    </local:ListBoxPage>

</Window>
