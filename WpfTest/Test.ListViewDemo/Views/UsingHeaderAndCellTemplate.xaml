<Window x:Class="Test.ListViewDemo.Views.UsingHeaderAndCellTemplate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Test.ListViewDemo.Views"
        mc:Ignorable="d"
        Title="UsingHeaderAndCellTemplate"
        Height="450"
        Width="800">

    <ListView SelectionMode="Single"
              ItemsSource="{Binding StudentList}"
              SelectedItem="{Binding CurrentSelectedStudent}">
        <ListView.View>
            <GridView>
                <GridViewColumn Header="Id" DisplayMemberBinding="{Binding Id}" />
                <GridViewColumn Header="姓名" DisplayMemberBinding="{Binding Name}" />
                <GridViewColumn Header="地址" DisplayMemberBinding="{Binding Address}" />
                <GridViewColumn>
                    <GridViewColumn.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="电子邮件*" />
                        </DataTemplate>
                    </GridViewColumn.HeaderTemplate>
                    <GridViewColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Email}"
                                       TextDecorations="Underline"
                                       Foreground="Blue"
                                       Cursor="Hand" />
                        </DataTemplate>
                    </GridViewColumn.CellTemplate>
                </GridViewColumn>
            </GridView>
        </ListView.View>
    </ListView>
</Window>
