<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Test.DocumentViewer">

    <Style TargetType="{x:Type local:XpsDocumentViewer}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="ShowPageBorders" Value="False" />
        <Setter Property="Margin" Value="0,40,12,0" />
        <Setter Property="Padding" Value="60,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:XpsDocumentViewer}">
                    <Border Focusable="False">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Padding="{TemplateBinding Padding}"
                                CanContentScroll="true" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
