<Window x:Class="Test.TransformDemo.LayoutTransformAdjust"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Test.TransformDemo"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Window.Resources>
        <local:OffsetConverter x:Key="OffsetConverter" />
    </Window.Resources>

    <Grid>
        <StackPanel>
            <Button x:Name="btn1"
                    Content="I AM Rotated 35"
                    Background="Yellow"
                    Width="{Binding Width}"
                    Height="{Binding Height}"
                    Margin="0,100,0,0"
                    RenderTransformOrigin="0.5,0.5">
                <Button.RenderTransform>
                    <RotateTransform Angle="{Binding Angle}" />
                </Button.RenderTransform>
            </Button>
            <Button Content="I not"
                    Width="100"
                    Height="30"
                    Margin="0,100,0,0" />
        </StackPanel>
        <StackPanel>
            <Button x:Name="btn2"
                    Content="I AM Rotated 35"
                    Background="Red"
                    Width="{Binding Width}"
                    Height="{Binding Height}">
                <Button.LayoutTransform>
                    <RotateTransform Angle="{Binding Angle}" />
                </Button.LayoutTransform>
                <Button.Margin>
                    <MultiBinding Converter="{StaticResource OffsetConverter}">
                        <Binding Path="Width" />
                        <Binding Path="Height" />
                        <Binding Path="Angle" />
                        <Binding Path="Region" />
                    </MultiBinding>
                </Button.Margin>
            </Button>
            <Button Content="I not"
                    Width="100"
                    Height="30"
                    Margin="0,100,0,0" />
        </StackPanel>
    </Grid>
</Window>
