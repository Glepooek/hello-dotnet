<Window x:Class="Test.DragBehaviorDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Test.DragBehaviorDemo"
        xmlns:behavior="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800"
        x:Name="root">

    <!--<Canvas x:Name="canvas">
        <Button x:Name="btn" Content="Mouse Over Me">
            <behavior:Interaction.Triggers>
                <behavior:EventTrigger EventName="MouseEnter" SourceObject="{Binding ElementName=btn}">
                    <behavior:ChangePropertyAction TargetObject="{Binding ElementName=root}"
                                                   PropertyName="Background"
                                                   Value="Red" />
                </behavior:EventTrigger>
                <behavior:EventTrigger EventName="MouseLeave" SourceObject="{Binding ElementName=btn}">
                    <behavior:ChangePropertyAction TargetObject="{Binding ElementName=root}"
                                                   PropertyName="Background"
                                                   Value="White" />
                </behavior:EventTrigger>
            </behavior:Interaction.Triggers>
        </Button>
    </Canvas>-->

    <!--<Canvas x:Name="canvas">
        <local:AnswerButton>
            <behavior:Interaction.Behaviors>
                <local:DragMoveBehavior1 Parent="{Binding ElementName=canvas}" DragFinished="DragMoveBehavior1_DragFinished_1" />
            </behavior:Interaction.Behaviors>
        </local:AnswerButton>
    </Canvas>-->

    <ItemsControl x:Name="test" ItemsSource="{Binding strings}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Grid />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <local:AnswerButton>
                    <behavior:Interaction.Behaviors>
                        <local:DragMoveBehavior1 Parent="{Binding ElementName=test}" DragFinished="DragMoveBehavior1_DragFinished_1" />
                    </behavior:Interaction.Behaviors>
                </local:AnswerButton>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>

    <!--<ItemsControl x:Name="test" ItemsSource="{Binding strings}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Grid />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <local:AnswerButton>
                    <behavior:Interaction.Behaviors>
    -->
    <!--  可拖动，但是移动的比较别扭  -->
    <!--<local:DraggableBehavior />-->

    <!--  无法拖动  -->
    <!--
                        <behavior:MouseDragElementBehavior />
                    </behavior:Interaction.Behaviors>
                </local:AnswerButton>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>-->
</Window>
