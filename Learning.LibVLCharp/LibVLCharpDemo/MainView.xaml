<Window x:Class="LibVLCharpDemo.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:s="https://github.com/canton7/Stylet"
        xmlns:uc="clr-namespace:LibVLCSharp.WPF;assembly=LibVLCSharp.WPF"
        Title="MainWindow"
        Width="800"
        Height="450"
        mc:Ignorable="d">
    <Grid>
        <uc:VideoView x:Name="VideoView"
                      Panel.ZIndex="1"
                      MediaPlayer="{Binding VLCPlayer}">
            <StackPanel x:Name="test" Orientation="Horizontal">
                <Button Width="50"
                        Height="25"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Bottom"
                        Command="{s:Action DoBeginPlay}"
                        Content="PLAY" />
                <Button Width="50"
                        Height="25"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Bottom"
                        Command="{s:Action DoStopPlay}"
                        Content="STOP" />
            </StackPanel>
        </uc:VideoView>
    </Grid>
</Window>
