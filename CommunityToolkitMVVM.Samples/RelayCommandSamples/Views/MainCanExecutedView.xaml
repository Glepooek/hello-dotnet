<Window x:Class="RelayCommandSamples.Views.MainCanExecutedView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RelayCommandSamples.Views"
        DataContext="{Binding MainCanExecutedViewModel, Source={StaticResource Locator}}"
        mc:Ignorable="d"
        Title="MainCanExecutedView"
        Height="450"
        Width="800">

    <Grid>
        <StackPanel HorizontalAlignment="Left">
            <Button Content="Control"
                    Width="150"
                    Height="40"
                    Command="{Binding ControlCommand}" />
            <Button Content="Submit"
                    Width="150"
                    Height="40"
                    Margin="0,10,0,0"
                    Command="{Binding SubmitCommand}" />
        </StackPanel>
    </Grid>
</Window>
