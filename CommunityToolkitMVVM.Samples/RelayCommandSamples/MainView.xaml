<Window x:Class="RelayCommandSamples.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RelayCommandSamples"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Window.Resources>
        <local:TaskResultConverter x:Key="TaskResultConverter" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Left">
            <TextBlock Text="{Binding Counter}"
                       HorizontalAlignment="Center"
                       FontSize="20" />
            <Button Content="test"
                    Width="100"
                    Height="40"
                    Command="{Binding IncreaseCounterCommand}" />
        </StackPanel>

        <StackPanel Grid.Row="1" HorizontalAlignment="Left">
            <TextBlock>
                <Run Text="Task status:" />
                <Run Text="{Binding DownloadTextCommand.ExecutionTask.Status, Mode=OneWay}" />
                <LineBreak />
                <Run Text="Result:" />
                <Run Text="{Binding Result, Mode=OneWay}" />
            </TextBlock>
            <Button Content="Click me!"
                    Width="200"
                    Margin="0,5"
                    HorizontalAlignment="Left"
                    Command="{Binding DownloadTextCommand}" />
            <ProgressBar Width="200"
                         Height="5"
                         IsIndeterminate="{Binding DownloadTextCommand.IsRunning, Mode=OneWay}" />
        </StackPanel>

    </Grid>
</Window>
